model Skill {
  id String @id @default(uuid())

  name        String  @unique
  icon        String?
  description String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  technician TechnicianProfile[]
  categories Category[]          @relation("CategorySkills")

  @@map("skills")
}
